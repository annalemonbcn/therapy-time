import { PropsWithChildren } from 'react'
import { StyledIconContainer } from './styles'

const ActiveIcon = ({ children }: PropsWithChildren) => <StyledIconContainer>{children}</StyledIconContainer>

export default ActiveIcon
